<a href="#" onclick="store(this)" data-payment_id="{{ $order->payment->id }}" data-url="{{ route('payment.store') }}" data-order_id="{{ $order->id }}" data-bs-toggle="modal" data-bs-target="#pay" class="btn btn-primary btn-sm mb-1 {{ $order->payment->status !== 'paid' ? '' : 'disabled' }}" style="width: 5rem"><i class="fal fa-upload"></i> Pay</a>
{{-- <a href="" class="btn btn-secondary btn-sm mb-1" style="width: 5rem "><i class="fal fa-download"></i> Print</a> --}}
<a href="#" onclick="detail(this)" data-proof_payment="{{ $order->payment->proof_payment }}" data-status="{{ $order->payment->status }}" data-amount_paid="{{ $order->payment->amount }}" data-order_id="{{ $order->id }}" data-bs-toggle="modal" data-bs-target="#detail-order" data-name="{{ Auth::user()->profile->name }}" data-address="{{ Auth::user()->profile->address }}" class="btn btn-success btn-sm mb-1" style="width: 5rem"><i class="fal fa-eye"></i> Detail</a>